import java.util.Arrays;

public class index {

  public static void main(String[] args) {
    /*
     * // Arrays unidimensional
     * 
     * int [] numeros = new int[5];
     * numeros[0]=5;
     * numeros[4]=7;
     * // imprimiendo contenido del array
     * System.out.println(Arrays.toString(numeros));
     * // imprimiendo la cantidad de numeros dentro del array
     * System.out.println(numeros.length);
     * Arrays.sort(numeros);
     * // Ordenando los numeros de menor a mayor
     * System.out.println(Arrays.toString(numeros));
     */

    // Arrays multidimensional

    /*
     * int [][][] numeros = new int[3][2][2];
     * numeros[0][0][0]=5;
     * System.out.println(Arrays.deepToString(numeros));
     * 
     */

    // Constantes

    /*
     * final String empresa = "hola mundo limited";
     * // empresa = "Chanchito feliz"; No va a reasigar el valor ya que final no
     * permite cambiar el valor ya que es una constante
     * empresa = "Chanchito feliz";
     * System.out.println(empresa);
     */

    /*
     * int x =15;
     * double y = 15.015;
     * int z = x +(int)y; //Si podemos realizar operaciones con variables de
     * diferentes tipos, pero debemos hacer un casteo explicito para evitar errores
     * de compilación.
     * System.out.println(z);
     * 
     * String j="1.1";
     * int k=5;
     * double l= Double.parseDouble(j)+k; // Usamos la clase parseDouble para
     * convertir un string a un double, no podemos solo poner (int)j
     * //tambien tenemos Byte.parseByte
     * //tambien tenemos Interger.parseInt
     * //tambien tenemos Float.parseFloat
     * // etc...
     * System.out.println(l);
     */

     
    // Math.

    /* 
     System.out.println(Math.abs(-15));// valor absoluto
     System.out.println(Math.ceil(10.0001));// redondea el numero hacia arriba
     System.out.println(Math.floor(10.999)); // redondea el numero hacia abajo
     System.out.println(Math.max(15, 25)); // devuelve el mayor
     System.out.println(Math.min(15, 25)); // devuelve el menor
     System.out.println(Math.round(15.5)); // redondea al mas cercano
     double random = Math.random(); // devuelve un numero aleatorio entre 0 y 1
     System.out.println((int)(random * 100)); // Aqui multiplicamos por 100 para obtener un numero entre 0 y 99 ademas agregamos para que sean numeros enteros sin decimales
     */

     



  }
}